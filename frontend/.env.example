# Frontend (Next.js) env — copy to .env.local and fill in.
# The backend URL the browser hits. Next.js also proxies /api/* to this in
# next.config.mjs rewrites, so the frontend can call same-origin /api/* in dev.
NEXT_PUBLIC_API_URL=http://localhost:8000
